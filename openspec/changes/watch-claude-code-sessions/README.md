# watch-claude-code-sessions

Generalise the watcher from Copilot-only to pluggable agent profiles and add a Claude Code profile: per-agent identity (command / name convention / bounded content fingerprint) and per-agent classification tokens, surfaced with an agent column in the TUI.
